@page
@model MusicLibraryManager.Pages.Albums.IndexModel

<main>

<header><h1>Albums</h1></header>

<hr />

@if (TempData["StatusMessage"] != null)
{
    <div class="alert alert-success">
        @TempData["StatusMessage"]
    </div>
}
<ul>
<a asp-page="Create" class="btn btn-primary">Create Album</a>
<a asp-page="/Artists/Index" class="btn btn-secondary">Back to Artists</a>
</ul>

<table class="table">

<thead>
<tr>
    <th>Name</th>
    <th>Year</th>
    <th>Artist</th>
</tr>
</thead>

<tbody>

@foreach (var album in Model.Albums)
{
<tr>
    <td>@album.Name</td>
    <td>@album.ReleaseYear</td>
    <td>@album.Artist?.Name</td>
    <td>
        <div class="table-buttons">
        <a asp-page="/Songs/Index" class="btn btn-primary" asp-route-albumId="@album.Id">Songs</a>  

        <a asp-page="Edit" class="btn btn-secondary" asp-route-id="@album.Id">Edit</a>

        <a asp-page="Delete" class="btn btn-danger" asp-route-id="@album.Id">Delete</a>
        </div>
    </td>
</tr>
}

</tbody>
</table>

</main>